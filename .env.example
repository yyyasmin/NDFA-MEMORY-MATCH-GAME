# Frontend: backend URL for API and Socket.IO
# Local dev: point to your backend (e.g. http://localhost:5000)
# Production: set this in Netlify → Site settings → Environment variables
VITE_BACKEND_URL=https://ndfa-memory-match-game.onrender.com
